---
description: Diagnose a layout, bug, or UI issue — read files, report findings, no fixes
---
Read the relevant files for this issue. Report:
1. Exact line numbers and class names involved
2. The full rendering/logic chain from outermost to innermost element
3. Root cause — what is actually causing the symptom
4. What would break if fixed naively

Do not change any code. Report findings only.

Files most likely relevant: src/pages/, src/components/Layout.tsx, src/components/BottomNav.tsx, src/components/Footer.tsx, src/components/Header.tsx
